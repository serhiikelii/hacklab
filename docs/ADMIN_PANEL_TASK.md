# Задача: Доработка админ панели

## 🎯 Новая логика управления

### Управление услугами (`/admin/services`)
1. **При входе:** Показать "Выберите категорию"
2. **После выбора категории:**
   - Список услуг для этой категории
   - Кнопка "Удалить" - удаляет услугу для ВСЕХ моделей в категории(если правильно понимаю поставит is active: false)
   - Кнопка "Добавить услугу" (вверху) - добавляет услугу для ВСЕХ моделей в категории.(если правильно понимаю логику БД то услуга сразу не появится на сайте а только пропишится в таблиццу)

   - **НЕ показывать** сами модели, только услуги

### Управление моделями (`/admin/models`)
При нажатии "Редактировать" на модели - 3 кнопки:
1. **"Редактировать картинку"** - форма на той же странице для замены/добавления картинки
2. **"Редактировать услуги"** - новая страница:
   - Список услуг для конкретной модели (цена, гарантия, время)
   - Кнопка "Редактировать" - редактирование на той же странице
   - Кнопка "Удалить" - удаляет услугу ТОЛЬКО для этой модели
   - Кнопка "Добавить услугу" (вверху) - новая страница с формой:
     - Название услуги(в форме випадающего списка доступних услуг для данной категории)
     slug в таблице цен формируется автоматически из анг названия услуги. При не заполненом поле гарантии значение по умолчанию 24 мес.
     - Цена
     - Длительность выполнения
     - Гарантия
3. **"Удалить"** - удаление модели
4. Добавить модель 
 в етой форме http://localhost:3000/admin/models/new добавить поле order где кратко описать что сортировка на сайте ведется относительно ордера(приоритет) и года. Чем больше ордер значит модель будет в конце списка и отобразить максимальное текущее значение order для данной категории (чтоб било понятно кге щас конец списка)

 ### Управление ценами
 оно мне в таком случае не нужно отдельно вообще убрать отсюда http://localhost:3000/admin так же убрать администратори управление

 ### История изменений 
 на етой странице http://localhost:3000/admin сейчас не работает но хотелось би чтоби при нажатии видеть историю изменений

## ✅ Что уже сделано

### Исправлено создание моделей
- **Проблема:** Хардкод категорий в `src/app/admin/models/new/page.tsx:89-92`
- **Решение:** Динамическая загрузка через `loadCategories()` из Supabase
- **Файл:** `src/app/admin/models/new/page.tsx:44-63`

### Удалены phantom поля
- **is_active** - не существовало в таблице prices
  - Удалено из: `src/app/actions/models.ts:184`, `src/app/actions/services.ts:182`
- **is_popular** - никогда не было в БД схеме
  - Помечено deprecated в: `src/types/pricelist.ts:44`

### Интегрировано управление ценами в услуги
- **Страница:** `src/app/admin/services/[id]/prices/page.tsx`
- **Кнопка "Редактировать"** в списке услуг ведет на управление ценами
- **Inline редактор** для price, duration_minutes, warranty_months

### Исправлены ошибки валидации и редиректа
- **image_url:** Добавлен `.or(z.literal(''))` для пустой строки - `models.ts:32`
- **redirect:** Убран из server action, теперь через useEffect - `models.ts:202-206`
- **placeholder года:** Изменен с "2024" на "Необязательно" - `new/page.tsx:230`

### Debug improvements
- Вывод ошибок валидации в UI - `new/page.tsx:117-126`
- Console.log в server action - `models.ts:94`

## ⚠️ На что обратить внимание (хардкод)

1. **Всегда загружать категории из БД динамически** - никогда не хардкодить options
2. **Проверять Zod схемы** - optional поля должны поддерживать null И пустую строку
3. **Не использовать redirect() в server actions с try/catch** - возвращать success и делать redirect на клиенте
4. **Проверять существование полей в БД** перед использованием в коде

## 📋 Следующие шаги

1. **Анализ архитектуры БД** с Context7 (Supabase)
2. Проверить возможность реализации новой логики
3. При необходимости внести изменения в схему БД
4. Реализовать новую логику управления услугами
5. Реализовать новую логику управления моделями

## 🔧 Требования к реализации

- **Обязательно использовать Context7** для изучения Supabase API
- **Не допускать хардкода** - все данные динамически из БД
- **Валидация на сервере** через Zod схемы
- **Proper error handling** с выводом ошибок в UI

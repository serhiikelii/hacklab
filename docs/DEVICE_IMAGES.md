# Работа с картинками устройств

## Как это работает

### 📦 Хранение
**БД хранит только ссылки**, а не сами файлы!

```sql
-- Таблица device_models
CREATE TABLE device_models (
  id UUID PRIMARY KEY,
  name TEXT NOT NULL,              -- "iPhone 15 Pro Max"
  slug TEXT UNIQUE NOT NULL,       -- "iphone-15-pro-max"
  image_url TEXT,                  -- "/images/devices/iphone/iphone-15-pro-max.webp"
  ...
);
```

### 📂 Структура файлов

```
public/
└── images/
    └── devices/
        ├── iphone/
        │   ├── iphone-15-pro-max.webp
        │   ├── iphone-15-pro.webp
        │   └── ...
        ├── ipad/
        │   ├── ipad-pro-12-9.webp
        │   └── ...
        ├── macbook/
        │   ├── macbook-air-m3.webp
        │   └── ...
        └── apple-watch/
            ├── apple-watch-series-9.webp
            └── ...
```

## Добавление новой картинки

### Вариант 1: Вручную (текущий способ)

**Шаг 1:** Добавить файл в папку
```bash
# Положить файл в правильную папку
public/images/devices/iphone/iphone-16-pro.webp
```

**Шаг 2:** Обновить БД
```sql
UPDATE device_models
SET image_url = '/images/devices/iphone/iphone-16-pro.webp'
WHERE slug = 'iphone-16-pro';
```

### Вариант 2: Через админ-панель (TODO - не реализовано)

**Что нужно реализовать:**
1. Загрузка файла через форму в админ-панели
2. Сохранение в `public/images/devices/{category}/`
3. Автоматическое обновление `image_url` в БД
4. Возможность заменить/удалить картинку

## Формат ссылок

### ✅ Правильно:
```
/images/devices/iphone/iphone-15-pro-max.webp
/images/devices/macbook/macbook-air-m3.webp
```

### ❌ Неправильно:
```
images/devices/iphone/iphone-15.webp          (без /)
/public/images/devices/iphone/iphone-15.webp  (public не нужен)
https://example.com/image.jpg                 (внешние ссылки)
```

## Рекомендации

### Формат файлов:
- **WebP** (приоритет) - лучшее сжатие
- PNG - для прозрачности
- JPG - для фото

### Именование:
- Используй slug модели: `iphone-15-pro-max.webp`
- Lowercase, через дефис
- Без пробелов и спецсимволов

### Размер:
- Ширина: 300-500px (для карточек)
- Оптимизация: сжатие без потери качества
- Размер файла: до 100KB

## Проверка отсутствующих картинок

```sql
-- Найти модели без картинок
SELECT slug, name, category_id
FROM device_models
WHERE image_url IS NULL OR image_url = '';
```

## Пример использования в коде

```typescript
// Получить модели с картинками
const { data: models } = await supabase
  .from('device_models')
  .select('slug, name, image_url')
  .eq('category_id', categoryId);

// Отобразить
models.map(model => (
  <img
    src={model.image_url || '/images/placeholder.png'}
    alt={model.name}
  />
));
```

## TODO: Функционал админ-панели

Нужно реализовать:
- [ ] Загрузка картинок через форму
- [ ] Предпросмотр перед сохранением
- [ ] Автоматическая оптимизация (resize, compress)
- [ ] Замена существующих картинок
- [ ] Удаление неиспользуемых файлов
- [ ] Bulk upload (загрузка нескольких файлов)

Вот Markdown файл, составленный на основе вашего описания. Я структурировал его по разделам для удобства работы.

```markdown
# 📋 Отчет по тестированию Админ-панели и список задач

**Текущий статус:** Основная проблема с правами доступа решена. Редактирование цен и замена картинок работает.
**Приоритет:** Высокий (исправление багов функционала).

---

## 1. Медиа и Картинки (Storage)

- [ ] **Именование файлов при загрузке**
  - **Проблема:** Сейчас при загрузке картинки имя файла формируется на основе `id`.
  - **Задача:** Изменить логику, чтобы имя файла формировалось на основе `slug` модели устройства (для SEO и читаемости).

- [ ] **Удаление картинок**
  - **Проблема:** При нажатии на красный крестик (удалить) появляется системное сообщение от `localhost` (вероятно, `window.alert` или необработанная ошибка), которого быть не должно в продакшн-интерфейсе.
  - **Задача:** Заменить на красивое модальное окно подтверждения или Toast-уведомление.

---

## 2. Управление услугами (Страница `/admin/catalog/[model]/services`)

Страница работает некорректно и требует доработки:

- [ ] **Сортировка (Order)**
  - **Проблема:** Поле `order` не отображается, и нет возможности его редактировать.
  - **Задача:** Вывести поле и добавить инпут/механизм для изменения порядка.

- [ ] **Тип услуги (Main vs Additional)**
  - **Проблема:** Все услуги отображаются как "Доп. услуга", хотя фактически находятся в главной категории.
  - **Задача:** Исправить логику отображения чекбокса или бейджа типа услуги.

- [ ] **Создание услуги**
  - **Проблема:** При попытке добавить новую услугу выпадает ошибка "Ошибка создания услуги".
  - **Задача:** Отладить API-запрос создания, проверить обязательные поля и ответ сервера.

- [ ] **Активность услуги (Toggle)**
  - **Проблема:** Ползунок "Активно/Неактивно" визуально есть, но при клике ничего не происходит.
  - **Задача:** Исправить обработчик события (`onClick` / `onChange`) для обновления статуса `is_active`.

---

## 3. Аудит Логи (`/admin/audit`)

- [ ] **Отображение логов**
  - **Проблема:** Визуальная или логическая ошибка отображения таблицы.
  - **Референс:** Скриншот `Screenshot 2025-11-26 090743.png`.
  - **Задача:** Проанализировать верстку или получение данных на странице аудита.

---

## 4. Управление Администраторами (Auth Logic)

**Концепция:** Отказ от UI создания юзеров в пользу Supabase Dashboard (Invite).

- [ ] **Синхронизация новых админов**
  - **Вопрос:** Как новый юзер, приглашенный через Supabase Email Invite, попадет в таблицу `public.admins` и получит права?
  - **Решение:** Необходимо реализовать **Database Trigger** на событие `INSERT` в таблице `auth.users`.
  - **Задача:** Написать триггер, который автоматически создает запись в `public.admins` при регистрации нового пользователя, либо добавить UI кнопку "Синхронизировать админа" для ручного добавления записи после регистрации.

---

## 5. Безопасность и Логин (Login Attempts)

- [ ] **Rate Limiting (Защита от перебора)**
  - **Проблема:** Сейчас, вероятно, срабатывает нативная защита Supabase (после ~10 попыток блок на время). Кастомная логика на 5 попыток отключена.
  - **Задача:** Вернуть ранее разработанную логику `check_rate_limit` (блок после 5 попыток).

- [ ] **Логирование попыток входа**
  - **Проблема:**
    1. Неудачные попытки пишутся.
    2. **Удачные входы (SUCCESS = TRUE) перестали записываться** в таблицу `login_attempts`.
  - **Задача:** Найти место в коде логина, где вызывается `record_login_attempt`, и убедиться, что она вызывается с параметром `true` при успешной авторизации.
```


✅ ПУНКТ 2 ПОЛНОСТЬЮ ЗАВЕРШЕН

  🎯 Реализовано:

  2.1 Order field ✅
  - Миграция: 041_add_order_to_category_services.sql        
  - Поле добавлено в category_services
  - VIEW обновлен
  - Drag-and-drop для изменения порядка

  2.2 Service Type ✅
  - Исправлен enum mismatch (main/extra)
  - Отображение корректное

  2.3 Создание услуги ✅
  - Исправлены баги в INSERT
  - service_type и order сохраняются в services
  (template)
  - order сохраняется в category_services

  2.4 Toggle активности ✅
  - Добавлен router.refresh()

  2.5 Drag-and-Drop (БОНУС) ✅
  - Установлен @dnd-kit
  - Создан ServicesList.tsx с полным drag-and-drop
  - Server Action updateServiceOrder()
  - Визуальные индикаторы перетаскивания
  - Автоматическое пересчитывание порядка (10, 20,
  30...)
  - Audit logging для изменений

  📁 Созданные/измененные файлы:

  Новые:
  1. migrations/041_add_order_to_category_services.sql      
  2. services/ServicesList.tsx

  Изменены:
  3. services/actions.ts (добавлен updateServiceOrder,      
  исправлен createService)
  4. services/page.tsx (использует ServicesList)
  5. services/ServiceToggle.tsx (добавлен
  router.refresh)
  6. services/AddServiceForm.tsx (исправлен enum)

  🎨 UX Features:

  - ✅ Drag handle с иконкой для перетаскивания
  - ✅ Visual feedback при перетаскивании (opacity,
  shadow)
  - ✅ Loading indicator при сохранении
  - ✅ Optimistic updates (UI обновляется сразу)
  - ✅ Error handling (откат при ошибке)
  - ✅ Hint text с инструкцией
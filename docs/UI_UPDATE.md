# ТЗ: Доработки UI и функционала сайта

Дата создания: *Текущая дата*
Статус: **В работе**

## 1. Секция социальных сетей (Header/Footer)

Необходимо добавить блок с иконками социальных сетей и ссылками-заглушками для дальнейшего заполнения.

**Требования:**
- **Платформы:** Telegram, Instagram, Facebook.
- **Расположение:** 
  - *Вариант А:* В "шапке" сайта (Header) справа или рядом с контактами.
  - *Вариант Б:* В "подвале" сайта (Footer) в блоке контактов.
  - *Вариант В:* В бургер-меню (для мобильной версии).
- **Функционал:** При клике должен происходить переход на соответствующую соцсеть (открывать в новой вкладке `target="_blank"`).
- **Реализация:** Использовать векторные иконки (SVG) или библиотеку иконок. Ссылки пока поставить как `#` или `https://instagram.com/placeholder`.

---

## 2. Аудит и исправление стилей на странице Apple Watch

**Проблема:** На странице конкретной модели (например, `/pricelist/apple-watch/apple-watch-9`) элементы UI (в частности, кнопки) имеют старую цветовую палитру, которая отличается от глобальной темы сайта.

**URL для проверки:**
`http://localhost:3000/pricelist/apple-watch/apple-watch-9` (саму модель я удалил поетому ссилка уже неактивно но вопрос актуален, если у модели отсутствуют услуги, или не прошел запрос от таблици услуги от БД то что тогда виводится на страницу?)

**Задачи:**
1. Проверить код компонента, отвечающего за рендеринг этой страницы (или кнопки).
2. Выявить причину расхождения цветов:
   - *Хардкод:* Стили прописаны инлайново или жестко заданы в отдельном CSS-файле.
   - *Кэширование/Сборка:* Изменения темы не подтянулись в этот модуль.
3. **Исправить:** Привести стили кнопок (цвет фона, ховер-эффекты, шрифт) в соответствие с глобальной дизайн-системой сайта. Убрать хардкод цветов, использовать CSS-переменные или тему (Theme Provider).

---

## 3. Замена "Гарантии" на "Время ремонта" в списке услуг

**Проблема:** Сейчас в карточке услуги отображается "Warranty: 24 mo.", что является неактуальной информацией. Необходимо выводить примерное время выполнения работ.

**Задачи:**
1. **Frontend:** Изменить лейбл "Warranty" на "Time" (или иконку часов).
2. **Логика отображения:**
   - Данные из БД приходят в минутах.
   - Необходимо написать функцию-хелпер для форматирования времени.

**Формат вывода (Примеры):**

| Значение из БД (минуты) | Отображение на сайте | Логика (примерная) |
| :--- | :--- | :--- |
| `30` | **30 min** | Если < 60 мин |
| `60` ... `120` | **1-2h** | Если >= 60 мин и < 24ч (округлять до часов или диапазонов) |
| `1440` (24ч) и более | **1-3d** | Если >= 24ч (конвертировать в дни) |

*Примечание для разработчика:*
Если в базе данных время указано жестко (например, 30), а нужно выводить диапазоны ("1-2h"), то логику диапазонов нужно зашить на фронтенде или добавить соответствующие поля в БД, если диапазоны сильно варьируются. 
**Приоритет:** Сделать функцию конвертации `minutes -> readable string` на клиенте.

4. Обработка ошибок загрузки изображений (Image Fallback)
Контекст проблемы:
В локальной папке public/images/devices все изображения были сконвертированы в формат .webp. Однако в базе данных ссылки остались старыми (например, .png или .jpg).
В результате на сайте изображения не загружаются (ошибка 404), но плейсхолдер (заглушка) не отображается. Вместо этого видна "битая" картинка или пустота.

Анализ причины:
Вероятно, текущая логика проверки выглядит примерно так:
{imageUrl ? <img src={imageUrl} /> : <Placeholder />}.
То есть проверка идет только на наличие строки в базе данных, но не обрабатывается ситуация, когда ссылка есть, а файл физически недоступен или имеет другое расширение.

Задачи:

Доработать компонент изображения:
Добавить обработчик события ошибки загрузки (onError).
Если изображение по ссылке из БД не загружается, автоматически подменять src на путь к дефолтному плейсхолдеру.
Алгоритм работы:
Попытка загрузить изображение по URL из БД.
Успех -> Показать изображение.
Ошибка (404/Network Error) -> Перехватить событие и принудительно отобразить компонент/картинку Placeholder.
Проверка: Убедиться, что при наличии в БД ссылки iphone-13.png (которого нет физически), сайт корректно отображает заглушку, а не пустое место.

5. Сортировка и упорядочивание карточек (Прайс-лист моделей)
Решение: Реализовать сортировку на уровне запроса к БД, используя существующее поле order (int4).

Задачи:

БД (Supabase): Заполнить колонку order для моделей. Логика: меньшее число = выше позиция в списке.
Backend/API: Модифицировать запрос получения данных, добавив первичную сортировку по порядку и вторичную (страховочную) по году выхода.

## 🚨 КРИТИЧЕСКОЕ ТРЕБОВАНИЕ (Архитектура)

**Строгий запрет на хардкод стилей.**
Для реализации всех визуальных элементов (цвета, отступы, типографика, кнопки) **обязательно использовать библиотеку `Context 7`**.
*   Не использовать инлайновые стили или кастомные hex-коды цветов.
*   Все переменные должны тянуться из дизайн-системы/контекста.